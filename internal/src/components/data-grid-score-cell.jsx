import React from 'react'
import PropTypes from 'prop-types'

export default function DataGridScoreCell() {
	return <div>@TODO</div>
}

DataGridScoreCell.propTypes = {
	value: PropTypes.oneOfType([null, PropTypes.number]).isRequired
}
