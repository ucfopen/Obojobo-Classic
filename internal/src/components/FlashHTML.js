import React from 'react'
import PropTypes from 'prop-types'

export default function FlashHTML({ onClick }) {
	return <div>@TODO</div>
}

FlashHTML.propTypes = {
	value: PropTypes.string.isRequired,
}
