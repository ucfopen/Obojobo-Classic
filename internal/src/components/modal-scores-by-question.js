import React from 'react'
import PropTypes from 'prop-types'

export default function ModalScoresByQuestion({ onClick }) {
	return <div>Skip this for now, not ready!</div>
}

ModalScoresByQuestion.propTypes = {}
