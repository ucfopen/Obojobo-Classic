import React from 'react'
import PropTypes from 'prop-types'

export default function MyInstances({ value }) {
	return <div>@TODO</div>
}

MyInstances.propTypes = {
	label: PropTypes.string.isRequired,
}
